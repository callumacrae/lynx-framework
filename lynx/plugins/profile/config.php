<?php

$config = array(
	/**
	 * Table to use if a table name is not specified in the get method
	 * params. If this is set to false, we will use the table used by the
	 * auth plugin instead (the users table, basically)
	 */
	'table'	=> false,
);